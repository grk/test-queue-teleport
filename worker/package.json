{
  "name": "tq-teleport-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-key": "bun run scripts/generate-key.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250410.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.14.0"
  }
}
